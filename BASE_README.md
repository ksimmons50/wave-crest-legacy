# Breezy Sites Base

A Next.js base application with terminal functionality and Docker support for both local development and CI/CD deployments.

It's a template that breezy sites project use to get started.

To speed things up, we try to run most things on the base docker image.