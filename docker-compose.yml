services:
  app:
    build: .
    container_name: breezy-sites-dev
    ports:
      - "3007:3007"  # Next.js app
    volumes:
      - .:/app
      - /app/node_modules
      - /app/.next
    environment:
      - NODE_ENV=development
    restart: unless-stopped
    stdin_open: true
    tty: true
